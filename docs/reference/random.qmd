# random { #primate.random }

`random`

## Functions

| Name | Description |
| --- | --- |
| [rademacher](#primate.random.rademacher) | Generates random vectors from the rademacher distribution.  |

### rademacher { #primate.random.rademacher }

`random.rademacher(size, engine='splitmix64', num_threads=1, seed=-1, dtype=np.float32)`

Generates random vectors from the rademacher distribution. 

#### Parameters

| Name   | Type         | Description                  | Default    |
|--------|--------------|------------------------------|------------|
| `size` | int or tuple | the output shape to generate | _required_ |